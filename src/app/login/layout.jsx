export const metadata = {
  title: "Sign In | Contextra",
  description: "Sign in to your Contextra reader account.",
  robots: { index: false, follow: false },
};

export default function LoginLayout({ children }) { return children; }
