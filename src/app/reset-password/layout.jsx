export const metadata = {
  title: "Reset Password | Contextra",
  description: "Choose a new password for your Contextra account.",
  robots: { index: false, follow: false },
};

export default function ResetPasswordLayout({ children }) { return children; }
